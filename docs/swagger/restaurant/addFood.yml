/restaurants/food/:
 post:
  security:
    - bearerAuth: []
  tags:
    - restaurants
  description: Add food to restaurant
  requestBody:
    content:
      'application/json':
        schema:
          type: object
          properties:
            restaurantId:
              type: number
              example: 2
              required: true
            foodId:
              type: number
              example: 5
              required: true
  produces:
    - application/json
  responses:
    200:
      description: Address has been added to restaurant.