/login:
 post:
   tags:
     - auth 
   description: Log In
   requestBody:
    content:
      'application/json':
        schema:
          type: object
          properties:
            email:
              type: string
              example: 'serverigaram@gmail.com'
              required: true
            password:
              type: string
              example: 'remidosol4434'
              required: true
   produces:
     - application/json
   responses:
     200:
      description: Logging in is successful.